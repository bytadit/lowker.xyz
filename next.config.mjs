/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["localhost", "https://img.clerk.com", "https://static.vecteezy.com", "static.vecteezy.com", "img.clerk.com"],
    },
  };

export default nextConfig;
